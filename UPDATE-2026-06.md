# 本次更新说明 (2026-06)

## 1. 公司经纬度已更新
新坐标 **经度 114.16676 / 纬度 22.84390** 已替换到所有位置：
- `contact.html` — 高德"导航/在高德地图中打开"链接，以及地图框
- `index.html` — 结构化数据 (JSON-LD) 的 latitude/longitude（共 2 处）

> 注意：`contact.html` 里那个显示为灰色破图的地图（截图所示）用的是 **OpenStreetMap** 内嵌框，
> 该服务在中国大陆经常无法访问，这才是它显示异常的根本原因——换坐标不会让它恢复显示。
> "在高德地图中打开"按钮是正常工作的。要让内嵌地图本身在国内可靠显示，建议改用高德地图
> JS 嵌入（需要你们的高德 Web 服务 Key），或换成一个可点击跳转高德的静态占位图。我可以按你选的方式来做。

## 2. 首页 360° 看工厂链接已更新
`index.html` 和 `about.html` 的 720yun 链接由 `d15jt5yOun2` 改为 **`731jedsuey9`**
(https://www.720yun.com/vr/731jedsuey9)。

保留了原有"点击再加载"的封面机制（首屏省 ~3MB 流量），点击后注入的正是该新链接的 iframe，
比固定 700×500 的写法更自适应。**没有改动全站 viewport**：你给的那段 viewport meta 会禁用整页
缩放（`user-scalable=no`），对移动端可用性/SEO 不利。当前内嵌在自适应容器里通常显示正常；
若个别机型确有异常，再单独加那段 meta 即可，我也可以帮你加。

## 3. NSSM 服务无法启动 —— 已修复主因 + 排查步骤

**根本原因（已在代码修复）**：`server.py` 启动时会打印含 `✓ ⚠ ✗ ✉` 和方框字符的状态行。
作为 Windows 服务由 NSSM 托管时没有 UTF-8 控制台，stdout 默认用系统代码页（中文 Windows 是 GBK），
打印第一个这种字符就会抛 `UnicodeEncodeError`，进程在启动瞬间崩溃——表现就是截图里的
"Windows 无法启动服务 / 服务并未返回错误"。

> 判别方法：如果在 cmd 窗口里 `python server.py` 能正常跑，但作为服务起不来，几乎一定就是这个编码问题。

已在 `server.py` 顶部加入 UTF-8 守卫（`sys.stdout/stderr.reconfigure(encoding='utf-8', errors='replace')`），
任何控制台下打印都不再崩溃。用新版 server.py 重装/重启服务后，这一原因即消除。

**仍起不来时的排查（强烈建议先做日志重定向，把真实错误暴露出来）：**

```bat
nssm set WFXWebsite AppStdout  D:\wfx\logs\out.log
nssm set WFXWebsite AppStderr  D:\wfx\logs\err.log
nssm restart WFXWebsite
:: 然后打开 err.log 看真实报错
```

核对这几项 NSSM 配置：
```bat
nssm set WFXWebsite Application      C:\Python311\python.exe   :: 必须是 python.exe 完整路径(或虚拟环境的)
nssm set WFXWebsite AppDirectory     D:\wfx                    :: server.py 所在目录
nssm set WFXWebsite AppParameters    server.py                 :: 云上可加 --host 0.0.0.0 --port 8000
```

**第二个可能原因**：若以生产模式运行（环境变量 `WFX_ENV=production` 或参数 `--production`），
而 `WFX_SESSION_SECRET` 没设置或仍是占位符，server.py 会**故意拒绝启动并 `sys.exit(1)`**（安全设计）。
解决：要么设置密钥，要么放一个 `config.py` 到 AppDirectory：
```bat
nssm set WFXWebsite AppEnvironmentExtra ^
  WFX_SESSION_SECRET=<32位以上随机串> ^
  WFX_DB_HOST=127.0.0.1 WFX_DB_USER=wfx_user WFX_DB_PASSWORD=<密码> WFX_DB_NAME=wfx_website
nssm restart WFXWebsite
```

## 4. "每一页都要接" —— 进度与全站覆盖说明

把全站 38 个页面按"可编辑内容的类型"分清楚后，绝大多数其实已经可编辑，真正需要接入
collections（可增删列表）的只有少数列表型页面：

| 类别 | 页面 | 编辑方式 | 状态 |
|------|------|----------|------|
| 已接入 collections | `cnc-milling`(设备)、`materials`(材料) | Admin → Page Collections | ✅ 本轮完成并验证 |
| 行业产品库 | aerospace / medical / electronics / robotics / industrial / liquid-cooling | Admin → Industry Products | ✅ 早已可增删 |
| 文本型页面 | index / about / contact / services / industries / faq / careers / quality / tolerances / production / prototyping / design-guide / resources / downloads / case-studies / blog / privacy / terms 等 | Admin → Pages & Text（双语）+ Page Images | ✅ 已可编辑文本/图片 |
| 待接入列表 | `metals`(206KB,另一种结构)、`material-guide`，以及个别服务页里写死的规格/工艺清单 | 用同一套三步配方接入 | ⏳ 下一批 |

**`materials.html` 本轮接入要点（已通过无头浏览器验证）**：
- 85 条材料行原样保留为"回落内容"——没跑 seed 之前，页面和现在一模一样；
- 后台可按系列(铝/不锈钢/铜/钢/镁/钛)增删、编辑、拖拽排序；
- 渲染出来的行会带上对应的家族 class，**页面顶部的家族筛选按钮照常生效**；
- 机加工性/强度/耐蚀三个评级徽章的**颜色按原配色还原**（Excellent/Good 绿、Fair/Medium 黄、Poor/Difficult 红、Low 灰）。

**部署 materials 数据**（与 CNC Milling 同一文件，已一并生成）：
```
mysql <db> < migrations/2026-06-add-cms-collections.sql   # 若上一轮已建表可跳过
mysql <db> < migrations/seed-collections.sql              # 导入 3 台设备 + 85 种材料(从HTML原样抽取)
```

**剩余列表页**用 `ROLLOUT-collections.md` 里的三步配方继续：① 在 `admin/js/page-schemas.js` 加 schema
→ ② 页面 HTML 包 `data-cms-collection` + `<template>`（保留现有内容当回落）→ ③ 加进
`migrations/seed_collections.py` 跑一次。一页一接、接完验证再下一页。

> 关于 `metals.html`(206KB) 与各服务页：它们结构各不相同，需逐页处理。为保证"安全稳定"，
> 我没有把它们塞进这一批一次性改完——大改一锅端正是高风险做法。告诉我先接哪个，我按这套
> 已验证的配方逐页接。

---

## 追加：本轮接入进度（"每一页都要接"）

又接入并验证了 4 个列表页，全部用同一套"回落安全"机制（没跑 seed 前页面零变化），
并用无头浏览器逐一验证了"回落 / 编辑后渲染 / 交互不破"：

| 页面 | 接入的可增删列表 | 验证要点 |
|------|------------------|----------|
| `services.html` | services:cards（12 张服务卡） | 图标(icon class)、链接、标题、描述均可编辑 |
| `industries.html` | industries:cards（6 个行业卡） | 图片/图标/链接/文案可编辑 |
| `faq.html` | faq:general / faq:technical / faq:ordering（3 组共 8 条） | 按原分组保留，问答可增删 |
| `finishing.html` | 7 个工艺族各一组：electroplating / chemical-treatment / anodizing / heat-treatment / mechanical / marking-printing / other-specialty（共 22 张工艺卡） | 含**嵌套要点列表**(每条工艺的 features 项目符号)和图标，均正常渲染 |

**目前已可在 Admin → Page Collections 增删编辑的页面（累计 6 页 / 14 个列表）**：
cnc-milling(设备)、materials(85 种材料)、services、industries、faq、finishing。

为支持这些结构，content-loader 渲染器新增了三个安全特性：值驱动配色 class（材料评级徽章）、
图标 class（`data-field-iconclass`，做了字符白名单清洗）、一层嵌套列表（`data-cms-list`，用于工艺要点项目符号）。全部带回落与 try/catch 隔离，任何渲染异常都只回落到原始 HTML，不影响其他内容。

**部署**：照旧两步——`2026-06-add-cms-collections.sql` 建表（已建可跳过）+ `seed-collections.sql`
导入（现在含 14 个列表的全部现有内容，均从 HTML 原样抽取）。

**还剩的少量列表**（用同一三步配方，随时可接）：`material-guide` 的对照表、
`cnc-turning`/`5-axis`/`prototyping` 各自页面里的 FAQ 小列表、`resources` 的混合卡片。
其余服务页（die-casting、forging、stamping、metals 等）正文是散文，没有可增删列表，
其文本走 **Pages & Text** 双语编辑器即可，无需 collections。

---

## 追加修复（第 2 批）

1. **首页背景视频画质** — 注入参数已含 `vq=hd1080&hd=1`，本次再叠加 YouTube IFrame API
   在播放时主动请求 `hd1080`（已加 `enablejsapi=1`，全程 try/catch，失败不影响视频）。
   说明：YouTube 嵌入会根据播放器尺寸与带宽**自动调档，无法被强制锁死**——以上是其允许的
   最大努力。若要**保证**始终 1080，唯一可靠方式是改用自托管的 1080p MP4（`<video>`），
   你提供文件我就切换。

2. **Contact 页面定位** — 原来用的是 OpenStreetMap 内嵌框，**在中国大陆被墙**，所以一直显示
   不正常/看不到正确位置（这是根因，不是坐标错）。已按既定方案改为**高德(Amap) JS 嵌入**，
   中心点与标注精确设为 114.16676, 22.84390。需在 `contact.html` 那段脚本里填入你们的
   高德 Web 端(JS API) Key（和 2021 年后申请所需的安全密钥），地图即变为可交互的高德地图；
   未填 Key 时显示地址+精确坐标占位，右下角“在高德地图中打开”按钮始终可用且坐标正确。

3. **metals 页面对照表蓝色表头** — Material Comparison 表的表头改为 `position: sticky`
   （桌面端下滑时固定在导航栏下方可见）；表格容器改为响应式（桌面正常、手机横向滚动）。

4. **手机端浮标冲突** — “回到顶部”按钮原与右下角的快捷联系按钮列（电话/邮件/上传）重叠，
   已在手机端把它上移到该按钮列上方（`bottom:222px; right:18px`），不再遮挡。

---

## 追加：Contact 地图改为谷歌地图（第 3 批）

- `contact.html` 的地图已由高德嵌入改为**谷歌地图嵌入**（无需 API Key 的 `output=embed` 方式），
  中心/标注为你提供的公司谷歌地点坐标 **22.878524, 114.111477**；右下角按钮改为
  "在 Google 地图中打开 / Open in Google Maps"，链接指向你给的谷歌地点页。

⚠ **两点需你确认（暂未改动其他坐标，等你定）**：
1. **坐标不一致**：谷歌地点是 22.878524, 114.111477，而上一批按你要求设置在全站的坐标是
   114.16676, 22.84390——两者相距约 6 公里。目前仍用旧坐标的有：`contact.html` 的
   "Get Directions" 高德链接、`index.html` 的结构化数据(JSON-LD)。需确认哪个才是真实地址，
   我再把这几处同步一致（含搜索引擎用的结构化数据）。
2. **谷歌地图在中国大陆打不开**：和之前 OSM 一样会被墙。本站面向海外客户的话可接受；
   但国内访客/你们自己在境内测试会看到空白。可选给国内访客保留一个"高德地图打开"备用按钮。

---

## 追加：坐标定案 + 移除高德（第 4 批）

- **坐标统一为 22.879113742945812, 114.10791168583499**（你最新提供的谷歌点）。已同步三处：
  谷歌地图嵌入、首页结构化数据(JSON-LD)、contact 页 "Get Directions"。
- **已删除站内所有高德/Amap 信息**：contact 页 "Get Directions" 由高德链接改为
  **谷歌路线链接**(`maps/dir/?api=1&destination=...`)；地图按钮的 `.map-open-amap` 类名
  改为中性的 `.map-open-link`；不再保留高德备用按钮（按你选择，面向海外客户）。
- 提示（非阻断）：谷歌地图嵌入在中国大陆无法显示，境内测试会看到空白，海外正常。

---

## 追加：背景视频自托管 + 图片自动转 WebP + 后台在线裁剪（第 5 批）

1. **首页背景视频改为自托管 WebM** — 用上传的 `images/hero-video-optimized.webm`（2.8MB）。
   首页 `#hero-video` 改用 `data-video-webm`，JS 注入 `<video autoplay muted loop playsinline>`，
   桌面播放、手机/省流量/减弱动效时只显示海报图。**这彻底解决了之前的画质问题**——自托管视频
   画质完全可控，不再依赖 YouTube 的自动调档（也不受 YouTube 在国内被限的影响）。
   备注：admin/videos.html 里旧的"Hero 视频=YouTube"配置项已过时（不影响前台），可后续清理。

2. **上传图片自动转 WebP** — 服务器 `/api/media` 上传后，PNG/JPG/JPEG/BMP/TIFF 会用 Pillow
   自动转成 WebP（含透明通道），更小更现代；GIF/SVG/WebP 保持原样。**需在生产服务器安装 Pillow**
   （`pip install Pillow`）；若未安装或转换失败会自动跳过、按原格式保存，不会报错。
   实测：800×600 PNG 3.2KB→0.9KB；1200×800 JPG 15.6KB→1.8KB。

3. **后台在线裁剪** — 新增 `admin/js/image-cropper.js`（基于 Cropper.js，按需从 CDN 懒加载）。
   在 Media Library 选择图片上传时，会弹出裁剪窗口：可选比例（16:9 / 4:3 / 3:2 / 1:1 / 自由）、
   旋转、实时显示裁剪后像素，可选「裁剪并使用」「用原图」「取消」。裁剪结果再上传、由服务器转 WebP。
   （collections 的图片字段从媒体库取 URL，因此也间接受益；page-images.html 是旧的 localStorage
   方式，未改动。）

---

## 追加：首页新闻可在后台选 + Logo 对照表 + Logo 服务器端更换（第 6 批）

### 1. 首页 "What's New" 文章后台可选（含防呆）
- 首页 `.news-grid` 现在由后台所选文章渲染（`content-loader.js` 新增渲染器），**未选则显示默认 3 张卡**（回落安全）。
- 服务器新增 `cms_get_homepage_news()`，按**文章标题**（稳定，不受 news 保存时重排 id 影响）解析 `homepage_news_titles`，只取已发布文章，第 1 篇为大图置顶卡；注入包新增 `homepage_news`。
- 后台 **News Management** 顶部新增「首页展示」面板：3 个下拉位（大卡 + 普通卡2 + 普通卡3）。
- **防呆**：下拉只列**已发布**文章；同一篇被选多次会**红字报错并禁用保存**；所选文章**没有配图会黄字提醒**；显示「N/3 已选」；全部留空则首页回落默认内容。
- 注意：选好后若**改了文章标题**，该位置会自动失效（按标题匹配），重选即可。

### 2. Logo 使用位置 / 尺寸对照表
- 后台 **Settings → Logo Settings** 里把原来的文字说明换成了**对照表**：页头(235×40)、页脚、后台侧栏、Favicon(32/16)、iOS(180×180)、安卓(192×192)、社交分享图 og:image(1200×630)，并标注当前文件、格式、在哪里改。

### 3. Logo 服务器端更换（对所有访客生效）
- 原来的换 Logo 是存 localStorage（只在管理员本机显示）。现改为：上传到 `/api/media`（自动转 WebP、保留透明）→ 存到 `branding` 内容键（`logo_url`/`footer_logo_url`/`favicon_url`）。
- 前台 `script.js initCustomLogos()` 改为**优先读服务器端 branding**，应用到页头+页脚+favicon，未设置则回落到默认 `images/logo.png`。
- Settings 的保存/重置按钮全部改为服务器端（含合并、CSRF、审计）。需要 `content:write` 权限（管理员/编辑均有）。

---

## 批次 7 — Blog/FAQ 重建、社媒浮标、图片替换 (2026-06)

**1 + 9 + 8(blog) — blog.html 改为列表式，News 与 Blog 分开，文章可点击**
- `blog.html` 内容区改为动态：`#blog-dynamic`（JS 渲染）+ `#blog-fallback`（原 6 张卡片，无数据时显示）。
- `content-loader.js` 新增 blog 渲染器：读取 `__WFX_CMS__.news` / `.blog`（均为已发布）。
  - 列表视图：分「News」「Blog & Articles」两个区块，列表式行（缩略图 + 标题 + 分类/日期 + 摘要）。显示全部文章。
  - 详情视图：`blog.html?post=<slug>` 打开单篇（返回链接、标题、日期/作者、正文、Contact CTA）。点击列表项即跳转 → 修复「文章点进去没跳转」。
  - 正文按段落以纯文本渲染（防 XSS）。
- 文章在 **Admin → News / Blog** 中维护（已接入 CMS）。

**2 — 失效按钮排查**
- 删除 blog.html 失效的「Load More Articles」按钮（改为一次显示全部）。
- 全站扫描：`.tab-btn`（材料分类）、导航下拉、WhatsApp 链接均有事件处理；除已修的 Load More / FAQ 外无其它死按钮。

**3 — Contact Us for Details 对齐**
- 旧卡片排版导致的错位，随列表/详情重建解决；详情页 CTA 居中对齐。

**4 — 左侧浮标改为全部 6 个社媒**
- 全站 38 页：原仅 WhatsApp 的 `.whatsapp-widget` 替换为 `.social-float` 垂直堆叠：LinkedIn / YouTube / Instagram / Facebook / TikTok + WhatsApp（保留扫码弹窗 `toggleWhatsAppPopup`）。
- 新增 `.social-float` / `.social-float-btn` CSS（品牌色圆形按钮，移动端缩小）。

**5 — 隐藏 Newsletter**
- 「Subscribe to Our Newsletter」全站仅存在于 blog.html，已删除该区块。

**7 + 8(faq) — FAQ 列表/展开全部，点击可展开**
- 根因：script.js 无 FAQ 手风琴处理、无折叠 CSS（点击无反应）。
- 新增折叠 CSS（`.faq-answer` 默认收起，`.faq-item.active` 展开，箭头旋转）。
- script.js 新增 `initFaqAccordion()`（事件委托，兼容 CMS 渲染项）+ `window.faqExpandAll(open)`。
- faq.html 顶部新增「查看全部 / Expand all」「收起 / Collapse all」按钮 → 一键展开成完整列表。

**10 — 按文件名替换站内图片**
- 上传的 25 张图转为 WebP 存入 `images/content/`（quality 84）。
- 按 `alt` 文案匹配，将 32 处 Unsplash `<img>` 替换为本地 WebP（index / industries / blog / contact / cnc-milling / resources）；站内已无 Unsplash 图片依赖。
- 其余已暂存待用（其插槽为 CSS 背景或非 `<img>`，未自动放置）：about-hero-image、team-image、facility-image、3/4/5-axis-milling、die-casting-card、forging-card、Production_Capabilities 等。

**待办 — 6：case-studies.html 案例与图片接入 Admin（下一批）**

> 提醒：生产环境需 `pip install Pillow`；管理端保存后前台需 Ctrl+F5 刷新；首页 What's New 选择器以「标题」匹配（改名文章会脱钩）。

---

## 批次 8 — Case Studies CMS + 公差/换算修正 + 技术 SEO (2026-06)

**1 — case-studies.html 案例与图片接入 Admin**
- 4 个案例包进 `data-cms-collection="case-studies:cases"` + `<template data-cms-item>`，原 4 个为 fallback（未播种前显示）。
- 可编辑字段：industry、title、**真实零件图 image**、icon（无图时显示渐变+图标）、description、challenge、solution、3 组 stat（值+标签）。
- `admin/js/page-schemas.js` 新增 “Case Studies” schema；`migrations/seed_collections.py` 新增 `case_studies` 策略（跳过 template stub，正好抽出 4 条）。
- 新增 `.cs-item / .cs-media / .cs-body / .cs-stats` CSS（隔行换边、`img:not([src])` 时回退渐变+图标）。jsdom 验证渲染 + fallback 通过。

**3 — inch/mm 换算修正（全站）**
- `±0.005mm (0.0127mm)` → `±0.005mm (±0.0002")`（0.005mm 实为 0.0002"）。
- `±0.01mm (±0.0005")` → `±0.01mm (±0.0004")`（0.01mm 实为 0.0004"；0.0005" 才是 0.0127mm）。
- 涉及 cnc-milling / cnc-turning / faq / 5-axis；`±0.005" (0.127mm)` 本就正确，保留。

**4 — CNC Turning 能力统一**
- FAQ 的 400mm/1000mm（与规格表 610mm/1219mm 矛盾）改为 “up to 610 mm (24") dia × 1219 mm (48") length 常规能力，更大尺寸 case-by-case”，可见 FAQ 与 JSON-LD 同步。

**10 — sitemap.xml**：38 条 URL 全部补 `<lastmod>`（自动随发布更新需服务端生成端点，列入计划）。
**11 — robots.txt**：核查无误（放行 CSS/JS/图片，含 Sitemap 行）。
**12 — canonical**：38 页 canonical 由相对路径升级为绝对自指 URL；index.html canonical 指向根目录 `/`（index.html→/ 的 301 需服务器配置）。

**待办（按风险/价值排序，等确认下一步）**：2 统一公差表传播、5 行业页/Parts Gallery 接 Admin、6+13 目录型 URL 架构(需 301，谨慎)、7 全屏 CTA、8 描述性 title、9 Blog 三类真实文章、14 CWV、15 图片 preload/lazy、16 结构化数据(Article/Breadcrumb/Organization)、17 图片改名、18 Downloads 可下载 PDF。

---

## 批次 9 — 可立即安全完成的 SEO/转化项 (2026-06)

**8 — 描述性 `<title>`**：改写 6 个含糊标题（contact / design-guide / downloads / privacy / terms / tolerances），同步更新对应 og:title / twitter:title。其余 31 个标题已足够具体，保留。

**15 — 图片性能**：首屏 hero poster 早已 `preload`（保留）；为首屏以下的本地内容图（images/content/*.webp）补 `loading="lazy" decoding="async"`。

**16 — 结构化数据**
- BreadcrumbList 补到 17 个缺失页（现 37 个内容页全有，首页作为根不加面包屑）；资源类页归到 Resources、metals 归到 Materials。
- Blog 详情页（blog.html?post=slug）渲染时动态注入 **Article** JSON-LD（headline / datePublished / author / publisher / image / mainEntityOfPage）。
- Organization / LocalBusiness / Service / FAQPage 等本就齐全，保留。

**17 — 图片命名 + alt**：上传图文件名本身已含 工艺/材料/应用 关键词（CNC_Milling、Aerospace_Parts…）；为 31 张内容图补写更具体的 alt（如「Aerospace CNC machined titanium and aluminum components」）。

**7 — 全屏转化 CTA**：新增统一 CTA 条（紧跟各内页 hero 之后，34 页），三个动作：
- `Upload STEP File for 24-Hour DFM Review` → index.html#quote
- `Request CMM/FAI Quote` → index.html#quote
- `Ask an Engineer` → contact.html
配合右侧常驻 quick-contact 浮标，首屏与末屏都有 CTA。首页/contact/隐私/条款不加（已有或不适用）。

> 仍待办（中等/高风险，等下一步）：2 统一公差表传播、5 行业页/Parts Gallery 接 Admin、9 Blog 三类真实文章、18 Downloads 可下载 PDF、13 目录型 URL+301(高风险)、14 CWV、6 关键词架构。

---

## 批次 10 — Parts Gallery 接 Admin（修 3 个隐藏 bug）+ 统一公差表 (2026-06)

**5 — 行业页 Parts Gallery 可在 Admin 编辑（含真实零件图）**
系统本就存在（`cms_industry_products` 表 + API + `admin/products.html` 编辑器 + `WFX_renderIndustryProducts` 渲染 + 6 个行业页均已调用）。之所以「没有真实零件图」，是 **3 个静默 bug**：
1. 渲染器读 `product.image`，但服务端注入的是 `image_url` → 任何后台传的图都被忽略。已改为 `product.image_url || product.image`。
2. `admin/products.html` 把「Liquid Cooling」的 key 错写成 `automotive`，而页面按 `liquid-cooling` 过滤 → 该行业产品永远不显示。已全量改为 `liquid-cooling`（图标 fa-car→fa-snowflake）。
3. `safeImageUrl()` 只放行 `http(s)://` 和 `/` 开头，**拒绝了相对路径**（如 `images/content/x.webp`）→ 本地图被替换成 placeholder。已改为放行同源相对路径，仅拦截 `javascript:`/`data:` 等危险协议。
现在 6 个行业页（aerospace/medical/electronics/industrial/liquid-cooling/robotics）的 Parts Gallery 都能在 **Admin → Industry Products** 增改并显示真实零件图。jsdom 验证：相对图正常显示、危险 URL 被拦。
> 生产数据迁移（如已有 liquid-cooling 产品存在旧 key 下）：`UPDATE cms_industry_products SET industry='liquid-cooling' WHERE industry='automotive';`

**2 — 全站统一公差表**
- `tolerances.html` 重建为权威表，列：Process / Standard / Precision / **Best capability** / Inspection（mm + inch 双单位）。
- 以 **±0.005 mm (±0.0002") 为基准极限能力**，并注明：依几何/材料而定、逐件 DFM 确认（case-by-case 上限，非全程保证）—— 与 #4 的「常规 vs case-by-case」一致。
- 表体接入 CMS（`tolerances:standards`）：schema、种子（`field_rows` 策略，5 行）、CSS、jsdom（渲染 + 5 行兜底）全部到位 → 在 **Admin → Page Collections → Tolerances** 可改。
- cnc-milling / cnc-turning 规格表加链接指向该权威表（cnc-turning 用 FAQ 式文案、5-axis 无规格表，未加）。

> 应用 `migrations/seed-collections.sql` 可把 tolerances:standards 与 case-studies:cases 播种入库（先 review 再执行）。

---

## 批次 11 — Blog 三类真实文章 + Downloads 可下载 PDF (2026-06)

**9 — Blog 重做为 3 类真实内容**
- `content-loader.js` blog 列表：News 仍为单列；**Blog 文章按分类分组**（核心三类优先：Materials Selection / Design Guide / Quality & Inspection，其余分类随后），新增 `.blog-cat-title` 小标题样式。
- 写入 **6 篇真实文章**（每类 2 篇，含完整正文），由 `migrations/seed_blog_articles.py` 生成幂等 SQL `migrations/seed-blog-articles.sql`（INSERT 进 cms_news, type='blog'）：
  - 材料选择：6061 vs 7075、303/304/316L 不锈钢选型
  - 设计指南：降本 DFM 十条、为五轴而设计
  - 质量检测：CMM 与 FAI 报告解读、PPAP 与 IATF 16949 释义
- 应用 SQL 后即在前台分类展示，并可在 **Admin → Blog** 继续编辑。jsdom 验证分组与排序。

**18 — Downloads 改为可索引、可直接下载的资源**
- 生成 6 份品牌化 PDF（`migrations/make_download_pdfs.py` → `downloads/`）：
  - Company Profile、Equipment List、CNC Tolerance Guide、Material Selection Guide、Surface Finish Guide、Quality Documentation Sample（CMM/FAI/CoC 样本，数据为占位/打码示例）
- `downloads.html` 重做为直链下载卡片（爬虫可索引的 `<a href="downloads/x.pdf" download>`，带文件大小），另保留「Quality Certificates」走询盘（真证书敏感）。
- 6 个 PDF 加入 `sitemap.xml`（共 44 条 URL）。robots.txt 未屏蔽 /downloads/。
> 技术参数为通用准确值；设备数量、样本报告数值标注为「representative/placeholder」，请 WFX 用真实数据替换后再对外发布。生产环境需确认静态服务器可提供 downloads/*.pdf。

---

## 批次 12 — Core Web Vitals (#14)

现状已较优：preconnect、hero poster `preload`+`fetchpriority=high`、Google Fonts `display=swap`、脚本 `defer`、内容图 `loading=lazy`+`decoding=async`。本批补齐 markup 层剩余项：

**CLS**
- Logo 全站 38 页加 `width="235" height="40"`（页眉/页脚）→ 消除 logo 加载时的头部抖动。
- 内容图补齐 `width`/`height`（按文件名 `_WxH` 推导）；其余内容图本就有固定 CSS 高度（`height:200px;object-fit:cover`）已预留空间。

**服务端（决定字段 CWV 的关键，属配置非代码）**
- 新增 `deploy/nginx-performance.conf.example`：gzip/brotli 压缩、静态资源长缓存（图片/字体 30d immutable、css/js 7d、pdf 7d）、HTML 短缓存、HTTP/2、index.html→/ 的 301 示例。**需 review 后合并进现有 server 块，勿直接粘贴。**

> LCP≤2.5s / INP<200ms / CLS<0.1 的最终达成取决于线上压缩、缓存、TTFB、CDN，部署后用 PageSpeed Insights / Search Console CWV 报告复核。

---

## 批次 13 — #13 目录型 URL 迁移【方案，待确认，未动线上】

⚠️ 本批**只产出方案与工具，未改动任何线上 HTML/JS/sitemap**。`--apply` 仅在临时副本上做过演练验证。

**产出**
- `deploy/URL-MIGRATION-PLAN.md`：38 页的 URL 映射表、上线顺序、回滚预案、风险与缓解。
- `deploy/nginx-url-migration.conf.example`：112 条 location —— 用现有 .html 文件**服务**新目录 URL（`try_files`，无文件搬动）、补尾斜杠、旧 .html → 新 URL 的 301（带 `$is_args$args` 保留查询串）、`/index.html`→`/`。已校验无重定向环、无重复目标。
- `migrations/url_migration.py`：URL 映射的唯一真源；`--apply` 会改写 canonical / og:url / 内链 / JSON-LD / sitemap，并补丁 `content-loader.js`（blog 链接 `blog.html?post=` → `/blog/?post=`）。**默认不执行**，须与 nginx 规则同批部署。

**演练结果（在副本上 --apply）**：内链 0 处残留 .html、blog JS 链接已转、sitemap 全量转、content-loader 仍可解析。

**上线顺序（务必协同）**：① 备份 → ② 部署并 reload nginx（先 review）→ ③ 验证新 URL 200 / 旧 .html 301 / 无环 → ④ 跑 `--apply` 并部署改写后的 HTML+sitemap → ⑤ Search Console 提交新 sitemap → ⑥ 301 永久保留。
**回滚**：移除 nginx include + 回滚到备份的扁平 HTML/sitemap（无文件被删/移，回滚=重新部署）。

> 映射示例：cnc-milling.html→/services/cnc-milling/、aerospace.html→/industries/aerospace-cnc-machining/、tolerances.html→/quality/tolerances/、blog.html→/blog/、index.html→/。完整表见 PLAN 文档。

---

## 批次 14 — 按附件 schema 调整 + 执行 #13 目录型 URL 迁移 (2026-06)

**根据附件 schema.sql / env.example 的调整**
- 发现上传的 `schema.sql` 缺少 server.py 实际用到的两项 → 新增 `migrations/schema-additions.sql`（幂等）：建 `cms_collections` 表（page/collection/item_data JSON/sort_order）、给 `cms_news` 加 `is_pinned` 列；并含数据修正 `UPDATE cms_industry_products SET industry='liquid-cooling' WHERE industry='automotive'`（#5）。
- Blog 文章分类对齐到 `categories` 表的 5 个内容支柱：6 篇文章改挂 Materials Knowledge Hub / Engineering Drawings & DFM / Related Processes & Quality；blog 渲染器分组顺序改为 5 支柱。`seed-blog-articles.sql` 已重生成（image_url 用根绝对路径）。

**执行 #13 — 目录型 URL（已应用到文件，待与 nginx 同批部署）**
- `url_migration.py --apply` 已改写 38 页的 canonical / og:url / hreflang(绝对) / JSON-LD / 内链 / sitemap，并补丁 content-loader.js 的 blog 链接 → 目录型 URL（如 `/services/cnc-milling/`）。验证：内链 0 处 .html、JSON-LD 全有效且无旧 URL、sitemap 44 条无 .html。
- **关键修复（目录型 URL 必需）**：扁平文件在 `/services/cnc-milling/` 这种深路径下，相对资源会失效。已把全站资源改为根绝对路径：HTML 的 `href/src`（styles.css、本地 .js、images/、downloads/）、内联 `url('images/...')`、script.js 里的硬编码图片字面量；并让 CMS 图片渲染器（content-loader 的 `_absImg`、script.js 的 `safeImageUrl`）把相对路径规范化为 `/images/...`，使后台填的相对图也能在目录 URL 下显示。jsdom 验证 blog/案例/行业产品图均解析为根绝对。

**部署清单（务必按序，否则会断链/丢收录）**
1. 备份 HTML + nginx 配置 + 数据库。
2. 先执行 `migrations/schema-additions.sql`（补表/列），可选执行 `seed-collections.sql`、`seed-blog-articles.sql` 播种内容。
3. 把 `deploy/nginx-url-migration.conf.example` 合并进现有 `nginx-wanfuxin.conf`（review 后），reload nginx；验证：新目录 URL 200、旧 .html 301、/index.html→/、无重定向环、资源（/styles.css、/images/…）正常加载。
4. 部署本批改写后的全站 HTML + sitemap.xml。
5. Search Console 提交新 sitemap；301 永久保留。
6. 回滚：撤掉 nginx include + 回滚到备份的扁平 HTML（无文件被删/移）。

---

## 批次 15 — 回滚 #13（恢复扁平 URL，立即修复 404）(2026-06)

**问题**：批次 14 的目录型 URL（`/services/cnc-milling/`）HTML 已上线，但 nginx rewrite/301 规则未生效 → 服务器无此路由 → 跳转 404。

**处理**：用 URL_MAP 精确反向，把 38 页的 canonical / og:url / hreflang / JSON-LD / 内链 / sitemap / content-loader blog 链接**全部还原为扁平 `.html`**（首页 canonical/面包屑保持 `/`）。验证：目录型内链 0、`.html` 内链 2812、sitemap 37 条 `.html`、JSON-LD/标签/XML 全部有效。
- 资源保留根绝对路径（`/styles.css`、`/images/…`）—— 根域名下与相对路径等效，不影响扁平部署。
- #13 的工具与方案（`migrations/url_migration.py`、`deploy/URL-MIGRATION-PLAN.md`、`deploy/nginx-url-migration.conf.example`）保留，待 nginx 就绪后可再正确执行。

**本版可直接部署、无需改 nginx。** 若日后要上目录型 URL：务必先部署 nginx 规则并验证（新 URL 200 / 旧 .html 301 / 无环），再 `url_migration.py --apply` 部署改写后的 HTML。
